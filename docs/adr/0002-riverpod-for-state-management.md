# Riverpod for state management

App state is managed with Riverpod, chosen over Provider (its non-compile-safe predecessor) and Bloc (team-scale ceremony a solo app doesn't need). Riverpod is the current default for new Flutter apps: compile-safe, testable without BuildContext, and simple enough for a small codebase while scaling if the app grows.
