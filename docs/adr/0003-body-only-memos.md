# Body-only memos, no title field

A Memo is a single body of plain text with no separate title. Most note apps ask for a title first, which raises the cost of capturing a thought; Memova is capture-first — open and type, and the list shows the first line of the body. The trade-off is accepted: no semantic title, so no search-by-title, no separate title sorting.
